import { Component } from '@angular/core';

@Component({
  selector: 'app-dispute-landing',
  templateUrl: './dispute-landing.component.html',
})

export class DisputeLandingComponent {
  constructor(
  ) {
  }
}
