meta {
  name: Create a new record on tco/v1 (POST)
  type: http
  seq: 15
}

post {
  url: {{v1_baseUrl}}/appearanceResultDataInsert
  body: none
  auth: inherit
}

example {
  name: 201 Response
  description: The successfully created record.
  
  request: {
    url: {{v1_baseUrl}}/appearanceResultDataInsert
    method: POST
    mode: none
  }
  
  response: {
    status: {
      code: 201
      text: Created
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
