meta {
  name: Auth Token
  type: http
  seq: 1
}

post {
  url: https://{{RSI_auth_host}}
  body: formUrlEncoded
  auth: none
}

headers {
  Content-Type: application/x-www-form-urlencoded
  User-Agent: insomnia/11.3.0
}

body:form-urlencoded {
  client_id: {{client_id}}
  client_secret: {{client_secret}}
  scope: openid
  grant_type: client_credentials
}

script:post-response {
  let data = res.body;
  
  bru.setEnvVar("bearer_token", data.id_token);
}

settings {
  encodeUrl: true
  timeout: 0
}
