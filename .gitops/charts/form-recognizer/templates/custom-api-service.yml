apiVersion: v1
kind: Service
metadata:
  name: {{ include "form-recognizer.fullname" . }}-custom-api
  labels:
    app.kubernetes.io/instance: {{ include "form-recognizer.fullname" . }}-custom-api
    app.kubernetes.io/name: {{ include "form-recognizer.fullname" . }}-custom-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: {{ include "form-recognizer.fullname" . }}-custom-api
    app.kubernetes.io/name: {{ include "form-recognizer.fullname" . }}-custom-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 5000
